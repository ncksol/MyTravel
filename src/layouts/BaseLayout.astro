---
interface Props {
  title: string;
  activeNav?: string;
}

const { title, activeNav } = Astro.props;

const navItems = [
  { href: '/', label: '🏠 Home', id: 'home' },
  { href: '/itinerary', label: '🗾 Itinerary', id: 'itinerary' },
  { href: '/restaurants', label: '🍣 Restaurants', id: 'restaurants' },
  { href: '/bookings', label: '📋 Bookings', id: 'bookings' },
  { href: '/budget', label: '💰 Budget', id: 'budget' },
  { href: '/practical-info', label: '📖 Practical Info', id: 'practical-info' },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} — Japan 2026</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@400;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
  </head>
  <body>
    <header>
      <div class="header-inner">
        <a href="/" class="logo">
          <span class="torii">⛩️</span>
          <span class="logo-text">Japan 2026</span>
        </a>
        <button class="menu-toggle" aria-label="Toggle menu">☰</button>
        <nav>
          {navItems.map((item) => (
            <a href={item.href} class:list={[{ active: activeNav === item.id }]}>
              {item.label}
            </a>
          ))}
        </nav>
      </div>
    </header>

    <main>
      <div class="container">
        <slot />
      </div>
    </main>

    <footer>
      <div class="wave-pattern"></div>
      <div class="footer-inner">
        <p>🌸 Japan Trip — September 19 to October 3, 2026 🌸</p>
      </div>
    </footer>

    <script>
      const toggle = document.querySelector('.menu-toggle');
      const nav = document.querySelector('nav');
      toggle?.addEventListener('click', () => {
        nav?.classList.toggle('open');
      });
    </script>
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
